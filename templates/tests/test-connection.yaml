apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cgwi-php.fullname" . }}-test-connection"
  labels:
    {{- include "cgwi-php.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cgwi-php.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
